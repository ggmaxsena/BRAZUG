{
  "name": "brazug-website",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node server.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "build": "npm run prisma:generate && node --check server.js && node --check lib/twitch.cjs && node --check lib/db.cjs && node --check lib/auth.cjs && node --check lib/admin-routes.cjs",
    "start": "node server.js",
    "check": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "express": "^4.21.2",
    "mysql2": "^3.6.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  }
}
