{
  "name": "brazug-website",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node server.mjs",
    "build": "npm run check",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check lib/twitch.mjs",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:reload": "pm2 reload ecosystem.config.cjs --update-env"
  }
}
