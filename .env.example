# Local: copie para .env
# Hostinger (georgesena.com): hPanel → Node.js → Settings and redeploy → Environment variables
#   (não use .env no Git — o painel injeta as variáveis)
# https://dev.twitch.tv/console/apps

TWITCH_CLIENT_ID=seu_client_id_aqui
TWITCH_CLIENT_SECRET=seu_client_secret_aqui
# PORT — na Hostinger é definido automaticamente; não defina PORT manualmente no painel
NODE_ENV=production
